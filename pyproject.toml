[project]
name = "coffee-agent"
version = "0.1.0"
description = "Dummy coffee agent repository for the alpha workspace."
requires-python = ">=3.10"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
pythonpath = ["src"]
